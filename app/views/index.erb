<h1><%= @title %></h1>

<ul>
  <% @meetups.each do |meetup| %>
  <li><a href = "/meetup/<%= meetup.id %>"><%= meetup.name %></a> -
        <%= meetup.description %>
        <%= ": \t" + meetup.location %></li>
  <% end %>
</ul>
